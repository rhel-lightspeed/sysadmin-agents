apiVersion: v1
kind: Service
metadata:
  name: sysadmin-agents
  labels:
    app: sysadmin-agents
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: sysadmin-agents
